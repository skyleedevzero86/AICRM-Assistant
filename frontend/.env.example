# Next.js dev proxy target (server-side rewrite in next.config.ts)
CORE_API_URL=http://localhost:8080

# Leave empty to call /api/* through the Next.js rewrite (recommended for local dev).
# Set to http://localhost:8080 only if you run without rewrites and rely on backend CORS (local profile).
NEXT_PUBLIC_API_BASE_URL=
